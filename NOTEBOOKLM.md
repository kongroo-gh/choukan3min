# NotebookLM で元ネタを作る

調査の裏取りと要約を NotebookLM に任せ、その出力を朝刊3分の原稿にする手順です。
Anthropic API は使いません。

## 役割分担

```
Claude   ソース候補を探す        ── URLを集める。中身の要約はしない
   ↓
あなた   NotebookLM に入れて回す ── ここだけ人の操作。APIが無いため自動化できない
   ↓
NotebookLM  要約・整理           ── 出典URL つきの6項目に整理させる
   ↓
Claude   原稿と画像に変換        ── content.json → PNG9枚 → 関門1 → 関門2
   ↓
GitHub   翌朝6:35に投稿          ── API不要。検査して投稿するだけ
```

**Claude に要約させず NotebookLM に要約させる意味**は、原稿を書く側と
一次情報を整理する側を分けられることです。`research.md` が Claude の手を
経ていない分、関門2の突き合わせが素直な検証になります。

---

## 前夜の手順

対象日は**投稿する朝の日付（JST）**です。前夜に作るので、ディレクトリ名は翌日になります。

### ① Claude にソースを集めさせる

Claude Code でこう頼みます。

```
明日の朝刊3分のソース候補を集めて。
経済ニュースを web で調べて、一次情報に近いURLを5〜8本、
それぞれ何の記事かを一行添えて出して。要約はしなくていい。
直近テーマと重複しないものを選んで。
```

Claude は URL の一覧を返します。**この段階では要約させません。**
整理は NotebookLM の仕事です。

### ② NotebookLM にソースを追加

https://notebooklm.google.com

ノートブックを開き、①のURLを**「ウェブサイト」として**追加します。
URL で入れておかないと、次で出典URLを書かせることができません。

### ③ プロンプトを貼って実行

対象日と直近テーマを埋め込んだプロンプトを作れます。

```bash
python3 scripts/notebooklm_prompt.py | pbcopy
```

引数を省くと翌朝の日付が使われます。日付を指定するなら `notebooklm_prompt.py 2026-08-30`。
そのまま NotebookLM に貼って実行してください。

### ④ 出力を research.md として保存

```bash
D=2026-08-29 && mkdir -p post/$D && pbpaste > post/$D/research.md && grep -c 'https\?://' post/$D/research.md
```

末尾に出るのが**出典URLの数**です。**2以上あることを必ず確認してください。**
NotebookLM は既定で [1][2] 形式の番号引用を付けますが、URL文字列が本文に
無いと `validate.py` が出典0件と判定して止まります。

### ⑤ Claude に原稿化を頼む

```
post/2026-08-29/research.md をもとに、朝刊3分の原稿を作って。
メモにない数字は入れないで。画像まで書き出して、関門1と関門2を通して。
```

以降の手順（文字数の目安、目視確認、review.json、commit）は
[MORNING.md](MORNING.md) にあります。

---

## NotebookLM を使わない日

用意しなかった日は、**Claude が自分で調査して `research.md` を書きます**。
2026-08-28 の回はこの方法で作りました。

品質は落ちませんが、**書き手と調査役が同じになる**ので、関門2の独立性は
その分下がります。`review.json` の `note` にどちらで作ったかを書き残してください。

---

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| `grep -c` が 0 か 1 | 番号引用だけで URL 文字列が出ていない。プロンプトの「URLをそのまま書く」を守らせる |
| 数字が入っていない | ソースが解説記事ばかり。統計局・日銀などの一次資料を足す |
| 前日と同じ話になる | `notebooklm_prompt.py` が直近テーマを自動で埋め込む。①で Claude にも重複回避を指示する |
| 出典どうしで数字が違う | **どちらかに寄せない。**両方の値と出典を `research.md` に残し、原稿でどちらを採ったか `review.json` に書く |

最後の項目は実際に起きました。2026-08-28 の回で7月の伸び率が
1.7%（東奥日報）と1.9%（日経の関連見出し）に見え、後者は全国・総合の値と
判断して1.7%を採り、その経緯を `research.md` と `review.json` に残しています。

---

## API を使う旧方式について

`scripts/generate.py` には、`research.md` を読んで過去の記録と突き合わせ、
素材が薄ければ web 検索で補う仕組みが入っています。**これは `ANTHROPIC_API_KEY`
が必要な旧方式で、いまの毎朝の運用では使いません。**

API に戻す場合はそちらが動きます。`scripts/digest.py`（投稿後の記録づくり）も
同様に API 前提です。
